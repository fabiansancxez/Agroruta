{
  "name": "agroruta-mvp",
  "private": true,
  "version": "1.0.0",
  "description": "MVP de AgroRuta: red inteligente de rescate, comercializacion y distribucion de excedentes de alimentos (Corabastos).",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n SERVER,CLIENT -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
